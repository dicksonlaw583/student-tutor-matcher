<!DOCTYPE html>
<html>
<head>
	<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
	<title>Student-Tutor Matching System</title>
	<%= stylesheet_link_tag "application", :media => "all" %>
  	<%= javascript_include_tag "application" %>
  	<%= csrf_meta_tags %>
</head>
<body>
	<div id="header" class="container_12">
		<div id="title">
			<%= image_tag "logo.jpg", :alt => "UTSC", :id => "logo" %>
			<h1>Student-Tutor Matching System</h1>
		</div>
		<%= render "layouts/login_box" %>
	</div>
	<div class="clear"></div>

	<% if signed_in? %>
		<%= render :partial => "#{current_user.role.name.pluralize}/nav" %>
	<% end %>

	<% unless notice.blank? %>
		<div id="message_notice" class="container_12"><%= notice %></div>
	<% end %>

	<% unless alert.blank? %>
		<div id="message_error" class="container_12"><%= alert %></div>
	<% end %>

	<% unless flash[:error].blank? %>
		<div id="message_error" class="container_12"><%= flash[:error] %></div>
	<% end %>
	
	<div id="content" class="container_12">
		<%= yield %>
	</div>
	<br class="clear" />
	
	<div id="footer" class="container_12">
		&copy; Team 11, 2012
	</div>
</body>
</html>
